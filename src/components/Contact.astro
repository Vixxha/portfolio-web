---
import portfolioData from "../data/portfolio.json";
const { contact } = portfolioData;
---

<section id="contact" class="py-20 px-4 max-w-3xl mx-auto text-center">
    <h2 data-aos="fade-down" class="text-4xl font-bold text-white mb-8">
        {contact.title}
    </h2>

    <div
        data-aos="zoom-in"
        class="bg-gradient-to-br from-zinc-900/50 to-zinc-900/80 backdrop-blur-sm border border-zinc-800/50 rounded-3xl p-8 md:p-12 shadow-2xl"
    >
        <p class="text-zinc-400 text-lg mb-8">
            ¿Tienes un proyecto en mente o simplemente quieres saludar? ¡Envíame
            un correo!
        </p>

        <a
            href={`mailto:${contact.email}`}
            class="group w-full max-w-lg mx-auto flex flex-col sm:flex-row items-center justify-center gap-3 px-6 py-4 sm:px-8 sm:py-5 bg-gradient-to-r from-indigo-600 to-violet-600 hover:from-indigo-500 hover:to-violet-500 text-white rounded-2xl font-bold text-base sm:text-lg transition-all hover:scale-[1.02] shadow-xl shadow-indigo-500/25 mb-12"
        >
            <div
                class="p-2 bg-white/10 rounded-full group-hover:bg-white/20 transition-colors"
            >
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    class="h-6 w-6"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
                    ></path>
                </svg>
            </div>
            <span class="break-all sm:break-normal">{contact.email}</span>
        </a>

        <div class="flex justify-center gap-6">
            {
                contact.socials.map((social) => (
                    <a
                        href={social.href}
                        target="_blank"
                        rel="noopener noreferrer"
                        class="text-zinc-400 hover:text-white transition-colors flex items-center gap-2"
                    >
                        {social.label}
                        <svg
                            xmlns="http://www.w3.org/2000/svg"
                            class="h-4 w-4"
                            fill="none"
                            viewBox="0 0 24 24"
                            stroke="currentColor"
                        >
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"
                            />
                        </svg>
                    </a>
                ))
            }
        </div>
    </div>
</section>
